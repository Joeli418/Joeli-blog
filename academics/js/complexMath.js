const cmplx = {
    r: 0,
    j: 0
  }

function execute() {
    const command = document.getElementById("command").value
    const answer = document.getElementById("answer")
    answer.innerHTML = "this is still a work in progress, Sorry!"
}

function cParallel(a, ja, b, jb) {

}